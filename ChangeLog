flibc 0.2.6:
	* change API for str_*trim functions. Add new arguments which contain the list of caracters to be trimed.
	* add str_trim_blanks, str_ltrim_blanks and str_rtrim_blanks
	* add \v caracter when trim blanks caracters
	* add str_startwith and str_endwith functions
	* add str_catf function
	* add str_replace function

flibc 0.2.5:
	* add io module

flibc 0.2.4:
	* add flibc/unit.h for unit test in C

flibc 0.2.3
	* don't check NULL arg in str_matches

flibc 0.2.2
	* avoid some macro redefinition

flibc 0.2.1
	* change api for log_open function (add more options)
	* remove log.c and switch to macro
	* add ENABLE_VT102_COLOR macro to enable color in term vt102

flibc 0.2.0
	* change api for str module (more simple, more visual)
	* implement new str function str_list_toarray

flibc 0.1.2
        * fix wrong macro call in log.h

flibc 0.1.1
	* add UNUSED() macro to mark variable not used (avoid compil warning)
	* put \0 in ssprintf and svsprintf function if libc function return -1
	* add tests/libtest.h in dist gzip
